@rendermode InteractiveServer

<MudThemeProvider Theme="@_theme" />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

@code {
    private MudTheme _theme = new MudTheme()
    {
        PaletteLight = new PaletteLight()
        {
            Primary = "#050a30",     // cor primária
            Secondary = "#f26444",   // cor secundária

        }
    };
}
